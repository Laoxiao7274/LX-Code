# Update server env (copy to .env)
PORT=8080
# 管理账号登录(页面用)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=a85676058
# CLI 发布 token(scripts/publish-release.mjs 用,与登录密码独立,留空则禁用 CLI 上传)
PUBLISH_TOKEN=change-me-please
# 公开下载基址(写进 latest.json 的 url;HTTPS 部署时填 https://你的域名)
PUBLIC_BASE_URL=http://localhost:8080
# 首页标题
SITE_TITLE=LXCode 更新服务
