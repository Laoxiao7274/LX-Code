# Update server env (copy to .env)
# 监听端口
PORT=8080
# 发布鉴权 token(发布脚本上传用),留空则禁止上传
PUBLISH_TOKEN=change-me-please
# 公开下载基址(写进 latest.json 的 url;无则用 Host 头推断)
# HTTPS 部署时填 https://你的域名,本地测试填 http://localhost:8080
PUBLIC_BASE_URL=http://localhost:8080
# 首页标题
SITE_TITLE=LXCode 更新服务
