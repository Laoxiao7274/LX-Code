fix: release 编译 open_devtools 用 cfg 属性(debug-only,release 不编译)

release 打包报错:webview.open_devtools() 是 dev-only 方法,release
编译时不存在。cfg!(debug_assertions) 宏在 release 求值 false 但方法
仍需类型存在,编译失败。改用 #[cfg(debug_assertions)] 属性真正排除
release 编译。

本地打包验证通过:
- LXCode_0.1.4_x64-setup.exe (23MB) ✓
- LXCode_0.1.4_x64-setup.exe.sig (updater 签名) ✓
